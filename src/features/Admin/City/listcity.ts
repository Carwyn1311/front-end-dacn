export interface City {
    id: number;
    name: string;
    province: number;
  }
  
export interface Province {
    id: number;
    name: string;
  }

  export let listcity: City[] = [];
  export let listpovince: Province[] = [];